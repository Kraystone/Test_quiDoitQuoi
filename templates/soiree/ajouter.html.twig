{% extends 'base.html.twig' %}

{% block body %}

    <h2>Ajouter une soirée</h2>
    {{ form(formulaire) }}

    <div>
        <a class="btn btn-warning" href="{{ path("home") }}">&lt; Retour à la liste</a>
    </div>

{% endblock %}